version: "3.8"

services:
  api:
    build:
      context: ./api
      target: production
    container_name: api
    depends_on:
      - postgres
    ports:
      - 8000:8000
  nginx:
    build:
      context: ./nginx
    container_name: nginx
    depends_on:
      - api
    ports:
      - 80:80
